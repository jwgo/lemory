"""Framework adapters: use a Lemory vault inside LangChain / LlamaIndex.

Soft dependencies — importing these modules requires the respective framework,
but Lemory itself never depends on them.

    from lemory.integrations.langchain import LemoryRetriever
    from lemory.integrations.llamaindex import LemoryLlamaRetriever
"""
